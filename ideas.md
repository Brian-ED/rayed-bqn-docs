pages:
naming convention

whenevr a function alocates memory, it's prefixed with "Load", fx, `font←LoadFont`. To unload it you can use UnloadFont


modifiers vs functions

you can open window with _openAs_, the 


(take docs from raylib)

reimplemented image.bqn